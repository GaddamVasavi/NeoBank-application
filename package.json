{
  "name": "neobank-application-monorepo",
  "version": "1.0.0",
  "description": "Enterprise Full-Stack Digital Banking Simulator",
  "private": true,
  "scripts": {
    "start": "docker compose up -d",
    "build": "cd frontend && npm run build && cd ../backend && mvn clean package -DskipTests",
    "test": "cd backend && mvn test && cd ../frontend && npm run test:run",
    "dev": "cd frontend && npm run dev",
    "lint": "cd frontend && npm run lint"
  },
  "devDependencies": {
    "typescript": "^5.5.3"
  }
}
